function square(x) {
    return;
   }
   console.log(square(4)); // undefined
   console.log(square(5)); // undefined
   
   let area = square(3);
   console.log(`Area is ${area}`); // Area is undefined
   
